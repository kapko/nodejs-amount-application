const postModelFields = {
    qrcode: Number,
    title: String,
    amount: Number,
    price: Number,
    totalAmount: Number,
    totalPrice: Number,
    sold: Number,
    soldPrice: Number,
    soldAmount: Number,
    balanceAmount: Number,
    balancePrice: Number,
}

module.exports = { postModelFields };
